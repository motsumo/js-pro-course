export default class weatherClass {
    constructor(country, temp_c, temp_f, text, icon) {
        this.country = country;
        this.temp_c = temp_c;
        this.temp_f = temp_f;
        this.text = text;
        this.icon = icon;
    }
}
